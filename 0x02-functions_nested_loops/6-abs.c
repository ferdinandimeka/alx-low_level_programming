#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 *@a: int to operate on.
 *
 * Return: success
 */
int _abs(int a)
{
	int n;
	int neg = -1;

	n = a * neg;
	if (a < 0)
	{
	return (n);
	}
	return (a);
}
