#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 *@a: int to operate on.
 *
 * Return: success
 */
int _abs(int n)
{
	int a;
	int neg = -1;

	a = n * neg;
	if (n < 0)
	{
	return (a);
	}
	return (n);
}
