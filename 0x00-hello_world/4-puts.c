#include <stdio.h>
#include <string.h>

/**
 * main - puts char
 *
 * return: always 0 (Success)
 */
int main(voidi)
{
char app[51];
strcpy(app, "Programming is like building a multilingual puzzle");
puts(app);
    return (0);
}

